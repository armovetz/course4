# path to trunk
INI_trunk_path = "../.."
INI_ti_dir_path = INI_trunk_path + "/data/time_intervals"
INI_dataset_name = "DI"

# interval's size in days
INI_time_interval = 200
INI_item_x_user_coef = 1

# manual borders switch
INI_manual_borders_on = False
INI_borders_file_name = "my_borders.lol"

# full-length intervals
INI_full_length_on = False

# comment (put 0 if no comment must be attached)
INI_comment = 0

# name of directory with generated time interval dirs.
# if == 0 then it will be generated as time string at the moment of generating
INI_case_dir_name = "my_borders_1try"

